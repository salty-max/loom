// Mini-notation parser.
//
// v0 scope (PICO-8 boundary): space-separated tokens with `~` for rests.
//   "c3 e3 ~ g3"  →  pattern of 4 steps
// Groups [x y], alternation <x y>, and choice | are explicitly out of v0.
export {};
